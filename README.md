# liverpool-players-flask

### Run Demo
- `pipenv shell`
- `pipenv install`
- Run dev server with `pipenv run dev`
- Run prod server with `pipenv run start`

### Available routes:
GET, POST: /api/players
GET, PATCH, PUT, DELETE: /api/players/:id

